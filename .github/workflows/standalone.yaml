name: Standalone CI

on:
  - workflow_dispatch
  # push:
  #   branches: [$default-branch]
  # pull_request:
  #   branches: [$default-branch]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        node-version: [14.x]

    environment: production

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: Cache node modules
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install node modules
        run: npm ci

      - name: Run webpack
        run: npm run standalone:webpack

      - name: Install NSIS
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: sudo apt install nsis

      - name: Pack Windows
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: npm run standalone:pack:win

      - name: Publish Windows
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: |
          npx oclif-dev publish -r pack -t win32-x64,win32-x86
          npx oclif-dev publish:win -r pack

      - name: Pack macOS
        if: ${{ matrix.os == 'macos-latest' }}
        run: npm run standalone:pack:macos

      - name: Publish macOS
        if: ${{ matrix.os == 'macos-latest' }}
        run: |
          npx oclif-dev publish -r pack -t darwin-x64
          npx oclif-dev publish:macos -r pack
