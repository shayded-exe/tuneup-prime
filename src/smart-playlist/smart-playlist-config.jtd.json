{
  "definitions": {
    "SmartPlaylist": {
      "properties": {
        "name": {
          "type": "string"
        },
        "rules": {
          "elements": {
            "ref": "PlaylistRule"
          }
        }
      },
      "optionalProperties": {
        "sources": {
          "elements": {
            "type": "string"
          }
        }
      }
    },
    "PlaylistRule": {
      "properties": {
        "field": {
          "ref": "StringFilterField"
        },
        "operator": {
          "ref": "StringFilterOperator"
        },
        "value": {
          "type": "string"
        }
      },
      "optionalProperties": {
        "caseSensitive": {
          "type": "boolean"
        }
      }
    },
    "StringFilterField": {
      "enum": [
        "album",
        "artist",
        "comment",
        "composer",
        "filename",
        "fileType",
        "genre",
        "label",
        "path",
        "remixer",
        "title"
      ]
    },
    "StringFilterOperator": {
      "enum": ["=", "!=", "contains", "excludes", "regex"]
    },
    "NumberFilterField": {
      "enum": ["bitrate", "bpm", "length", "rating", "year"]
    },
    "NumberFilterOperator": {
      "enum": ["=", "!=", "<", "<=", ">", ">="]
    }
  },
  "properties": {
    "smartPlaylists": {
      "elements": {
        "ref": "SmartPlaylist"
      }
    }
  }
}
